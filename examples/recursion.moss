let recursive = |depth: Int|: Void => {
    if depth <= 0 {
        print_line("Bottom");
    } else {
        print_line("Recursing: " + str(depth));
        recursive(depth - 1);
    };
};

recursive(5);