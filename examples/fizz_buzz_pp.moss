let fizz_buzz = |n: Int|: List<Str> => {
    mut count = 0;
    let fizz_buzz_list: List<Str> = [];

    loop {
        let item = count;

        push(fizz_buzz_list, item);

        count = count + 1;

        if count == n {
            break;
        };
    };

    fizz_buzz_list;
};

let result = fizz_buzz(10);
print_line(result);
