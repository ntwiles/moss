let add = (a: Int, b: Int): Int => {
    a + b;
};

let sub = (a: Int, b: Int): Int => {
    a - b;
};

let mul = (a: Int, b: Int): Int => {
    a * b;
};

let result = mul(sub(add(3, 2), 1), 2);

print(result);